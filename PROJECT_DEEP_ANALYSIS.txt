========================================================================================================================
PROJECT DEEP ANALYSIS REPORT
========================================================================================================================
Generated At: 2026-04-21
Project Root: C:\Users\ASUS\Documents\Backend\Loan-Discovery-Web-Application

1) REPOSITORY OVERVIEW
The repository contains a full-stack loan discovery application with a React/Vite frontend in [loan/](loan/) and a Spring Boot backend in [backend/](backend/).

2) CURRENT TECH STACK
- Frontend: React 19, Vite, React Router, Tailwind CSS, Framer Motion, Lucide, Recharts, Three.js.
- Backend: Spring Boot 3.2.5, Java 21, Spring Web, Spring Data JPA, Spring Security, Validation, JWT, MySQL Connector.
- Tooling: Maven wrapper, PowerShell on Windows, ESLint, Vite build pipeline.

3) FRONTEND STATE
- Routes: `/`, `/login`, `/signup`, `/dashboard`.
- Landing experience: premium hero, interactive rates, market sections, and fintech styling.
- Auth experience: login and signup pages with shared form wrappers and alerts.
- Dashboard experience: profile, loan profile, eligibility, EMI calculator, comparison, recommendations, IFSC lookup, and admin analytics tabs.
- Shared UI system: premium cards, buttons, inputs, tables, alerts, loading states, empty states, section headers, and page containers.
- Session handling: browser-stored JWT sessions with protected routes.

4) BACKEND STATE
- Auth: JWT register and login flow with BCrypt password hashing.
- Profiles: personal profile save/load and loan profile save/load.
- Comparisons: save, list, and delete comparisons.
- Admin: stats endpoint for counts, recent signups, loan-type popularity, and averages.
- Bank data: IFSC lookup, bank search, branches by city, and bank name listing.
- Market data: global hotspot endpoint using live macro-data enrichment and cached seed data.

5) BACKEND API MAP
- `POST /api/auth/register`
- `POST /api/auth/login`
- `GET /api/profile/me`
- `PUT /api/profile/personal`
- `PUT /api/profile/loan`
- `GET /api/comparisons`
- `POST /api/comparisons`
- `DELETE /api/comparisons/{id}`
- `GET /api/admin/stats`
- `GET /api/ifsc/{code}`
- `GET /api/banks/search`
- `GET /api/banks/by-city`
- `GET /api/banks/names`
- `GET /api/global-market/hotspots`

6) CONFIGURATION CONTRACT
- Backend MySQL URL comes from `DB_URL` with a localhost fallback.
- Backend credentials come from `DB_USERNAME` and `DB_PASSWORD`.
- JWT signing uses `JWT_SECRET` and token lifetime uses `JWT_EXPIRATION_MS`.
- Admin access uses `ADMIN_EMAIL` in the backend and `VITE_ADMIN_EMAIL` in the frontend.
- Frontend API base URL comes from `VITE_API_BASE_URL` with `http://localhost:8080/api` as the default.

7) LOCAL RUNNING NOTES
- Backend run pattern on Windows PowerShell:
  - Set the environment variables.
  - Run `backend/.\mvnw.cmd spring-boot:run`.
- Frontend run pattern:
  - Run `npm install` once in [loan/](loan/).
  - Run `npm run dev` for local development.
  - Run `npm run build` for production verification.

8) VERIFIED STATUS
- Backend compile had already succeeded with the Maven wrapper.
- Frontend production build succeeded after the CSS repair during the UI polish pass.
- The backend remains MySQL-only; no H2 fallback should be introduced.

9) RECENT IMPLEMENTATION AREAS
- Global theme tokens and premium panel styling.
- Shared UI primitives across the app.
- Landing page hero, interactive rates, navbar, footer, auth screens, and dashboard shells.
- Backend auth/profile/comparison/admin service and controller layers.

10) OPERATIONAL RISKS
- Backend startup still depends on valid MySQL credentials and an accessible local database.
- If the admin tab does not show, the auth user email must match the admin env var.
- If protected routes bounce to login, the browser auth session should be cleared and re-created.

11) FOLLOW-UP AREAS
- JWT auth hardening and token lifecycle checks.
- Frontend auth integration polish.
- Saved comparison UI refinement and admin analytics review.
- Production hardening and deployment documentation.
